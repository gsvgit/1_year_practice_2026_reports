\documentclass[a4paper]{article}

\usepackage[a4paper, top=8mm, bottom=8mm, left=8mm, right=8mm]{geometry}
\usepackage{float}
\usepackage{graphicx}
\usepackage{caption}

\usepackage{polyglossia}
\setdefaultlanguage[babelshorthands=true]{russian}
\setotherlanguage{english}

\usepackage{fontspec}
\setmainfont{FreeSerif}
\newfontfamily{\russianfonttt}[Scale=0.7]{DejaVuSansMono}

\usepackage[tiny, compact]{titlesec}

\usepackage{titling}
\setlength{\droptitle}{-1cm}
\pretitle{\begin{center}\begin{bfseries}\Large}
\posttitle{\par\end{bfseries}\end{center}}
\preauthor{\begin{center}\normalsize}
\postauthor{\par\end{center}\vspace{-1.8cm}}

\usepackage[hidelinks]{hyperref}
\usepackage{bookmark}
\usepackage{csquotes}
\usepackage{booktabs}
\usepackage{multirow}

\emergencystretch=2em

\title{Сравнение форматов хранения разреженных матриц: COO и QuadTree}

\author{Нарышев Сергей Иванович}

\date{}

\begin{document}

\maketitle

\begin{flushright}
    Группа: \emph{2025.Б-72-мм}

    Кафедра: \emph{кафедра системного программирования}

    Научный руководитель: \emph{Григорьев Семён Вячеславович}

    Номер семестра практики: \emph{2}
\end{flushright}

\section{Введение}

Разреженные матрицы~--- матрицы, в которых большинство элементов равны нулю,~--- встречаются повсеместно: в задачах вычислительной математики, анализе графов, обработке изображений и машинном обучении. Хранить такую матрицу в плотном формате~--- двумерном массиве~--- неэффективно: память тратится на нули, а операции над матрицей вынуждены обходить бесполезные элементы.

Существует множество форматов компактного хранения разреженных матриц. Среди наиболее распространённых~--- CSR (Compressed Sparse Row), CSC (Compressed Sparse Column) и COO (Coordinate List). CSR и CSC оптимизированы для последовательного доступа к строкам или столбцам, но предполагают статичную структуру: добавление или удаление элементов требует перестроения внутренних массивов. COO~--- простейший формат, в котором каждый ненулевой элемент задаётся тройкой (строка, столбец, значение). Он удобен для построения матрицы и передачи данных между библиотеками; в данной работе тройки хранятся в отсортированном массиве, и поиск элемента выполняется бинарным поиском.

В проекте QTreeFSharp~\footnote{URL: \url{https://github.com/Lamagraph/QTreeFSharp} (дата обращения: 15.07.2026).} используется альтернативный подход~--- дерево квадрантов (quadtree). Матрица представляется бинарным деревом, каждый внутренний узел которого содержит четыре поддерева, соответствующих четвертям матрицы. Если все четыре поддерева одинаковы, они сжимаются в один лист. Такая структура естественным образом обеспечивает разреженное хранение и позволяет эффективно выполнять рекурсивные операции (умножение матриц, отображение, свёртку), поскольку операция применяется только к занятым поддеревьям.

QTreeFSharp~--- это библиотека разреженных векторов и матриц на языке F\#~\footnote{URL: \url{https://en.wikibooks.org/wiki/F_Sharp_Programming} (дата обращения: 15.07.2026).}, реализующая базовые операции линейной алгебры и алгоритмы анализа графов в парадигме GraphBLAS~\footnote{URL: \url{https://graphblas.github.io/} (дата обращения: 15.07.2026). }~--- модели вычислений, в которой линейная алгебра используется как язык для работы с графами.

\section{Постановка задачи}

В исходном проекте единственным полноценным форматом хранения разреженных матриц был quadtree. Тип \texttt{CoordinateList} при этом существовал и использовался как промежуточное представление при импорте данных (многие библиотеки~--- SuiteSparse, SciPy, MATLAB~--- используют CSR или COO как формат обмена), однако набор операций над COO-матрицами отсутствовал, что делало формат непригодным для практического использования и сравнения с quadtree.

Кроме того, не было данных о том, как quadtree сравнивается с традиционными форматами по производительности. Для одних операций (доступ по индексу, умножение матриц) дерево может оказываться быстрее за счёт кэширования и сокращения числа обходимых элементов, для других (построение, обновление)~--- медленнее из-за накладных расходов рекурсии и аллокаций.

\section{Цель работы}

Целью работы является развитие формата COO (Coordinate List) в библиотеке QTreeFSharp~--- выделение его в отдельный модуль с полным набором операций~--- и сравнение производительности с существующим форматом quadtree на синтетических и реальных данных, а также сравнение двух возможных представлений формата COO: на массиве и на связном списке.

\section{Задачи работы}

Для достижения цели были поставлены следующие задачи.

\begin{enumerate}
    \item Реализовать модуль COO с базовыми операциями: доступ к элементу (\texttt{cooGet}), обновление элемента (\texttt{cooUpdate}), отображение (\texttt{cooMap}, \texttt{cooMapi}), поэлементное отображение двух матриц (\texttt{cooMap2}, \texttt{cooMap2i}) и умножение матриц (\texttt{mxmcoo}).
    \item Реализовать мостовые функции для конвертации между форматами COO и quadtree (\texttt{fromCoordinateList}, \texttt{toCoordinateList}).
    \item Написать модульные тесты для проверки корректности реализованных операций.
    \item Разработать инфраструктуру измерения производительности на базе BenchmarkDotNet для сравнения двух форматов.
    \item Провести измерения на синтетических данных (разреженных и плотных матрицах различных размеров).
    \item Провести измерения на реальных матрицах из коллекции SuiteSparse~\footnote{URL: \url{https://sparse.tamu.edu/} (дата обращения: 15.07.2026).}.
    \item Реализовать вторую версию формата COO на связном списке (модуль \texttt{COOList}) и сравнить представления на массиве и на списке.
\end{enumerate}

\section{Актуальность и область применения}

Разреженные матрицы составляют основу большинства задач вычислительной линейной алгебры. Выбор формата хранения напрямую влияет на скорость выполнения и потребление памяти. Результаты сравнительного анализа полезны разработчикам библиотек линейной алгебры и вычислительных пакетов при выборе внутреннего представления данных.

Проект QTreeFSharp является прототипом реализации вычислений с помощью interaction nets~--- формальной модели параллельных вычислений. Сравнение с традиционным форматом COO позволяет понять, какие операции целесообразно реализовывать на деревьях квадрантов, а какие~--- на плоских структурах данных.

\section{Описание решения}

\subsection{Формат COO}\label{subsec:coo}

Формат COO (Coordinate List)~--- простейший формат хранения разреженных матриц. Каждый ненулевой элемент матрицы представляется тройкой: номер строки, номер столбца и значение. Все тройки хранятся в отсортированном по номерам строк и столбцов массиве.

В библиотеке QTreeFSharp тип \texttt{CoordinateList} (а также типы троек \texttt{COOEntry} и функции конвертации \texttt{fromCoordinateList}, \texttt{toCoordinateList}) уже присутствовал в модуле \texttt{Matrix} и использовался как промежуточное представление при импорте данных. В рамках данной работы формат COO был выделен в отдельный модуль \texttt{COO}, дополнен набором операций над матрицами и использован для сравнительного анализа с quadtree. Основной тип~--- \texttt{CoordinateList}, содержащий количество строк, количество столбцов и отсортированный массив троек \texttt{COOEntry}. Типы строк и столбцов помечены единицами измерения F\# (\texttt{rowindex}, \texttt{colindex}), что позволяет системе типов предотвратить смешивание индексов разных размерностей.

Операции над матрицей в формате COO.

\begin{itemize}
    \item \texttt{cooGet}~--- поиск элемента по индексу. Выполняет бинарный поиск по отсортированному массиву троек. Сложность в худшем случае~--- $O(\log n)$, где $n$~--- число ненулевых элементов.
    \item \texttt{cooUpdate}~--- обновление значения элемента. Массив троек поддерживается в отсортированном порядке; при обновлении позиция находится бинарным поиском, после чего элемент либо заменяется, либо вставляется в массив со сдвигом последующих элементов.
    \item \texttt{cooMap}~--- применение функции к каждому элементу. Если функция возвращает \texttt{None} для отсутствующих элементов (нормальный режим), обрабатываются только существующие тройки. Если функция возвращает значение для отсутствующих элементов, выполняется полный перебор всех позиций матрицы~--- этот режим используется редко и работает медленнее.
    \item \texttt{cooMapi}~--- аналог \texttt{cooMap}, но функция получает также индексы строки и столбца.
    \item \texttt{cooMap2}, \texttt{cooMap2i}~--- поэлементное отображение двух матриц. Списки троек обеих матриц проходятся одновременно; при совпадении индексов функция получает оба значения, при несовпадении~--- только одно.
    \item \texttt{mxmcoo}~--- умножение матриц. Реализовано два алгоритма: оптимизированный (группировка по строкам для операций, поглощающих \texttt{None}) и общий (полный перебор всех комбинаций строк и столбцов).
\end{itemize}

Наряду с реализацией на массиве разработана вторая версия формата COO~--- модуль \texttt{COOList}, где тройки хранятся в связном списке F\#. Список всегда поддерживается отсортированным: \texttt{cooUpdate} находит позицию вставки линейным проходом и пересобирает список через \texttt{cons} без копирования блоков памяти, а \texttt{cooGet} выполняет линейный поиск (\texttt{List.tryFind}). Такое представление лишено бинарного поиска (у списка нет произвольного доступа за $O(1)$), но обновление не требует выделения нового массива и физического сдвига элементов. Обе версии используют общий тип \texttt{CoordinateList} в представлении на массиве и собственный тип \texttt{ListCOO} на списке; между ними реализована взаимная конвертация.

\subsection{Формат QuadTree}

В формате quadtree матрица представляется деревом типа \texttt{qtree}. Лист (\texttt{Leaf}) содержит либо \texttt{Dummy} (пустая область), либо \texttt{UserValue(Some v)} (ненулевой элемент) или \texttt{UserValue(None)} (область, заполненная нулями). Внутренний узел (\texttt{Node}) содержит четыре поддерева: NW (северо-запад), NE (северо-восток), SW (юго-запад), SE (юго-восток).

Ключевой механизм~--- конденсация: если после операции все четыре потомка узла оказались одинаковыми листьями, они сливаются в один. Это обеспечивает компактность структуры для данных с повторяющимися блоками.

Операции над матрицей в формате quadtree.

\begin{itemize}
    \item \texttt{get}~--- доступ к элементу. Выполняется рекурсивный спуск по дереву: на каждом уровне определяется, в какой квадрант попадает искомый индекс, и выбирается соответствующее поддерево. Сложность~--- $O(\log N)$, где $N$~--- размер стороны матрицы.
    \item \texttt{set}~--- обновление элемента. Аналогичный рекурсивный спуск с заменой листа на нужной глубине и пересчётом числа ненулевых элементов. Возвращает новую матрицу (функциональная немутабельность).
    \item \texttt{map}, \texttt{mapi}~--- рекурсивное отображение с обходом только занятых поддеревьев.
    \item \texttt{map2}, \texttt{map2i}~--- рекурсивное отображение двух матриц, совмещённых по структуре дерева.
    \item \texttt{mxm}~--- умножение матриц по алгоритму divide-and-conquer: матрица делится на четыре подматрицы, выполняются восемь рекурсивных умножений и четыре сложения подматриц.
\end{itemize}

\subsection{Мостовые функции}

Для конвертации между форматами реализованы две функции.

\begin{itemize}
    \item \texttt{fromCoordinateList}~--- преобразование COO в quadtree. Список троек рекурсивно разделяется на четыре квадранта, строится дерево с последующей конденсацией.
    \item \texttt{toCoordinateList}~--- преобразование quadtree в COO. Рекурсивный обход дерева, собирающий все листья с ненулевыми значениями в массив троек.
\end{itemize}

\section{Модульные тесты}

Для проверки корректности реализованных операций написаны модульные тесты с использованием фреймворка xUnit, а также property-based тесты с использованием библиотеки FsCheck. Всего написано 194 теста, из которых 39~--- для модуля COO. Тесты покрывают.

\begin{itemize}
    \item \texttt{cooGet}: поиск существующего элемента, отсутствующего элемента и обращение за пределы матрицы (выбрасывается \texttt{System.ArgumentOutOfRangeException}, что соответствует идиоматике .NET).
    \item \texttt{cooUpdate}: вставка нового элемента, обновление существующего, вставка в начало и конец списка.
    \item \texttt{cooMap} и \texttt{cooMapi}: отображение с сохранением и удалением элементов, а также полный режим с заполнением отсутствующих позиций (в том числе с зависящей от индексов функцией).
    \item \texttt{cooMap2} и \texttt{cooMap2i}: поэлементное отображение совпадающих и несовпадающих матриц.
    \item \texttt{mxmcoo}: умножение матриц с различными операциями сложения и умножения, включая случай, когда операция умножения поглощает \texttt{None} (оптимизированный путь), и случай, когда не поглощает (общий путь).
    \item Границы: пустые матрицы, матрицы нулевого размера, матрицы размером $1 \times 1$, матрицы с одним ненулевым элементом.
\end{itemize}

Property-based тесты (проверяются на широком диапазоне произвольно сгенерированных входных данных, включая отрицательные индексы и повторяющиеся координаты):
\begin{itemize}
    \item согласованность \texttt{get} для Quadtree и COO в каждой ячейке матрицы;
    \item сохранение всех значений и размеров при конвертации \texttt{fromCoordinateList} \(\rightarrow\) \texttt{toCoordinateList};
    \item корректность \texttt{cooUpdate}: записанное значение и изменение длины списка (вставка либо замена);
    \item согласованность \texttt{set} (Quadtree) и \texttt{cooUpdate} (COO) при записи в одну и ту же ячейку;
    \item операция \texttt{cooMapValues} применяется ровно к одному сохранённому значению каждого элемента;
    \item выход за границы матрицы в \texttt{cooGet}, \texttt{cooUpdate}, \texttt{get} и \texttt{set} приводит к исключению \texttt{ArgumentOutOfRangeException}.
\end{itemize}

Все 194 теста проходят успешно.

Покрытие модуля \texttt{COO.fs} измерялось инструментом Coverlet (формат Cobertura): покрытие строк~--- порядка 95\%, покрытие ветвлений~--- порядка 85\%; наибольшее покрытие (\texttt{cooGet}, \texttt{cooMap}, \texttt{cooMapi}, \texttt{cooUpdate})~--- 100\% строк.

\section{Экспериментальная оценка}

\subsection{Инструменты и окружение}

Измерения проводились с использованием BenchmarkDotNet~\footnote{URL: \url{https://benchmarkdotnet.org/} (дата обращения: 15.07.2026).}~--- фреймворка для микробенчмарков .NET. Среда исполнения~--- .NET 10. Операционная система~--- Windows 11. Процессор~--- AMD Ryzen 7 5800HS (3.20\,ГГц, 8 физических ядер, 16 логических). Каждая операция выполнялась в профиле \emph{ShortRun}: один запуск (LaunchCount=1) с тремя прогревочными и тремя измеряемыми итерациями; BenchmarkDotNet вычисляет математическое ожидание времени (Mean) и стандартное отклонение (StdDev). Все эксперименты (синтетические разреженные, плотные и реальные матрицы) измерялись в пяти независимых запусках; итоговое значение~--- среднее по пяти запускам с межзапусковым стандартным отклонением.

Визуализация результатов выполнялась с помощью Matplotlib~\footnote{URL: \url{https://matplotlib.org/stable/contents.html} (дата обращения: 15.07.2026).}.

\subsection{Эксперимент~1: Синтетические разреженные матрицы}

Сгенерированы синтетические квадратные матрицы трех размеров: $256 \times 256$, $512 \times 512$ и $1024 \times 1024$. Плотность заполнения~--- 1\% и 5\% (доля ненулевых элементов). Матрицы случайным образом заполнялись ненулевыми значениями.

Сравнивались операции: \texttt{map} (отображение), \texttt{mapi} (отображение с индексами), \texttt{map2}, \texttt{map2i} (поэлементное отображение двух матриц), \texttt{get} (доступ к элементу), \texttt{set} (обновление элемента), \texttt{mxm} (умножение матриц).

На рис.~\ref{fig:sparse_map_1}--\ref{fig:sparse_get_1} приведены результаты для матриц с плотностью~1\%.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/sparse_map_fill1.png}
        \captionof{figure}{Время выполнения \texttt{map} на синтетических разреженных матрицах (плотность~1\%). Ось~Y~--- время в микросекундах (логарифмическая шкала).}
        \label{fig:sparse_map_1}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/sparse_mapi_fill1.png}
        \captionof{figure}{Время выполнения \texttt{mapi} на синтетических разреженных матрицах (плотность~1\%).}
        \label{fig:sparse_mapi_1}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/sparse_get_fill1.png}
        \captionof{figure}{Время выполнения \texttt{get} на синтетических разреженных матрицах (плотность~1\%).}
        \label{fig:sparse_get_1}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/sparse_set_fill1.png}
        \captionof{figure}{Время выполнения \texttt{set} на синтетических разреженных матрицах (плотность~1\%).}
        \label{fig:sparse_set_1}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/sparse_mxm_fill1.png}
        \captionof{figure}{Время выполнения \texttt{mxm} на синтетических разреженных матрицах (плотность~1\%).}
        \label{fig:sparse_mxm_1}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/sparse_mapi_fill5.png}
        \captionof{figure}{Время выполнения \texttt{mapi} на синтетических разреженных матрицах (плотность~5\%).}
        \label{fig:sparse_mapi_5}
    \end{minipage}
\end{figure}

Результаты для матриц с плотностью~5\% аналогичны по характеру для остальных операций, но абсолютные значения времени выше пропорционально числу ненулевых элементов.

По результатам измерений на разреженных синтетических матрицах:
\begin{itemize}
    \item \textbf{\texttt{map}, \texttt{mapi}, \texttt{map2}, \texttt{map2i}, \texttt{mxm}:} COO быстрее quadtree во всех случаях. На матрице $1024\times1024$ с плотностью~1\% COO быстрее в операции \texttt{map} в 51 раз (0.51\,мс против 26.0\,мс), в \texttt{mapi}~--- в 6.2 раза (14.4\,мс против 88.7\,мс), в \texttt{map2}~--- в 9.3 раза (6.1\,мс против 56.3\,мс), в \texttt{map2i}~--- в 7.4 раза (18.3\,мс против 134.8\,мс), в \texttt{mxm}~--- в 25 раз (289\,мс против 7.3\,с). Группировка троек по строкам в \texttt{mxmcoo} даёт перебор только ненулевых пар ($O(n_1 \cdot \bar{n}_2)$), тогда как рекурсивный алгоритм quadtree в таких данных не получает выигрыша от конденсации.
    \item \textbf{\texttt{get} и \texttt{set}:} quadtree быстрее, но после перехода COO на бинарный поиск разрыв по \texttt{get} практически исчез. На матрице $1024\times1024$ с плотностью~1\% \texttt{get} сопоставимы: 32.2\,мкс у COO против 28.7\,мкс у quadtree (quadtree быстрее лишь в 1.1 раза); \texttt{set}~--- quadtree быстрее в $\sim$$2.5\cdot10^3$ раз (0.35\,мс против 0.88\,с). Ожидаемо: доступ и обновление в дереве выполняются за $O(\log N)$, тогда как COO выполняет бинарный поиск по отсортированному массиву за $O(\log n)$, а обновление дополнительно требует вставки со сдвигом за $O(n)$.
\end{itemize}

\subsection{Эксперимент~2: Плотные матрицы}

Для оценки поведения форматов при максимальной нагрузке проведены измерения на полностью заполненных матрицах размеров $64 \times 64$, $128 \times 128$ и $256 \times 256$. Результаты приведены на рис.~\ref{fig:dense_map}--\ref{fig:dense_mxm}.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/dense_map.png}
        \captionof{figure}{Время выполнения \texttt{map} на плотных матрицах. Ось~Y~--- время в микросекундах (логарифмическая шкала).}
        \label{fig:dense_map}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/dense_mapi.png}
        \captionof{figure}{Время выполнения \texttt{mapi} на плотных матрицах.}
        \label{fig:dense_mapi}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/dense_get.png}
        \captionof{figure}{Время выполнения \texttt{get} на плотных матрицах.}
        \label{fig:dense_get}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/dense_set.png}
        \captionof{figure}{Время выполнения \texttt{set} на плотных матрицах.}
        \label{fig:dense_set}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/dense_mxm.png}
        \captionof{figure}{Время выполнения \texttt{mxm} на плотных матрицах.}
        \label{fig:dense_mxm}
    \end{minipage}
\end{figure}

На плотных данных проявляется специализация форматов. В операциях \texttt{get}, \texttt{set} и \texttt{mxm} quadtree быстрее COO. На матрице $256\times256$ \texttt{get} сопоставимы: 2.51\,мс у quadtree против 4.08\,мс у COO (в 1.6 раза): quadtree спускается по $\log N$ уровням, а COO~--- по $\log n$ троек. Операция \texttt{set} в COO пересоздаёт отсортированный массив при каждом обновлении (вставка со сдвигом за $O(n)$): на $256\times256$~--- 8.52\,с против 17.2\,мс у quadtree (почти в 500 раз). В операции \texttt{mxm} quadtree также быстрее: 1.41\,с против 8.01\,с (в 5.7 раза)~--- плотная структура даёт выигрыш от деления на подматрицы и конденсации. Напротив, в операциях \texttt{map} и \texttt{mapi} COO быстрее: на $256\times256$~--- 13.4\,мс против 19.1\,мс (в 1.4 раза) и 12.7\,мс против 19.3\,мс (в 1.5 раза) соответственно. При полной плотности quadtree вынужден хранить и обходить почти все $N^2$ листьев, что сводит на нет преимущество конденсации, тогда как COO обрабатывает $n=N^2$ троек с меньшими константными накладными расходами.

\subsection{Эксперимент~3: Реальные матрицы}

Для оценки производительности на данных реальной структуры использовались матрицы из коллекции SuiteSparse Matrix Collection~--- крупнейшей базы данных разреженных матриц, применяемых в вычислительной математике~\footnote{Дистрибутив матриц загружен из репозитория коллекции: \url{https://github.com/DrTimothyAldenDavis/SuiteSparse} (дата обращения: 19.09.2026).}. Загружены десять матриц различного размера, плотности и происхождения. В перечне указан размер матрицы и число ненулевых элементов в COO-представлении (для симметричных матриц учитываются оба треугольника).

\begin{itemize}
    \item \textbf{bcspwr01}~--- энергосистема, $39\times39$, 170 элементов.
    \item \textbf{arc130}~--- материаловедение, $130\times130$, 1282 элемента.
    \item \textbf{bcspwr05}~--- энергосистема, $443\times443$, 2066 элементов.
    \item \textbf{west0479}~--- химическая инженерия, $479\times479$, 1910 элементов.
    \item \textbf{494\_bus}~--- энергосистема, $494\times494$, 2160 элементов.
    \item \textbf{dwt\_992}~--- конструкционные расчёты, $992\times992$, 17\,736 элементов.
    \item \textbf{jagmesh7}~--- сетка (конечные элементы), $1138\times1138$, 8588 элементов.
    \item \textbf{bcspwr06}~--- энергосистема, $1454\times1454$, 6754 элемента.
    \item \textbf{cryg2500}~--- кристаллография, $2500\times2500$, 12\,349 элементов.
    \item \textbf{zenios}~--- задача оптимизации портфеля, $2873\times2873$, 30\,064 элемента.
\end{itemize}

Матрицы были загружены в формате Matrix Market и преобразованы в оба внутренних формата: COO и quadtree. Для каждой матрицы выполнялась одна и та же выборка из 1000 случайных элементов (общая для COO и quadtree), используемая в операциях \texttt{get} и \texttt{set}. Результаты приведены на рис.~\ref{fig:real_summary}--\ref{fig:real_mxm}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\textwidth]{images/real_summary.png}
    \caption{Сводная диаграмма: время выполнения пяти операций на десяти реальных матрицах из коллекции SuiteSparse. Левая колонка~--- COO, правая~--- quadtree. Ось~Y~--- время в микросекундах (логарифмическая шкала).}
    \label{fig:real_summary}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/real_map.png}
        \captionof{figure}{Время выполнения \texttt{map} на реальных матрицах.}
        \label{fig:real_map}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/real_mapi.png}
        \captionof{figure}{Время выполнения \texttt{mapi} на реальных матрицах.}
        \label{fig:real_mapi}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/real_get.png}
        \captionof{figure}{Время выполнения \texttt{get} на реальных матрицах.}
        \label{fig:real_get}
    \end{minipage}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/real_set.png}
        \captionof{figure}{Время выполнения \texttt{set} на реальных матрицах.}
        \label{fig:real_set}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.32\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/real_mxm.png}
        \captionof{figure}{Время выполнения \texttt{mxm} на реальных матрицах.}
        \label{fig:real_mxm}
    \end{minipage}
\end{figure}

\subsection{Сравнение двух представлений COO: массив и список}

Помимо сравнения с quadtree измерено, что даёт выбор внутреннего представления формата COO. Обе версии из разд.~\ref{subsec:coo} выполняют одни и те же операции, но используются разные асимптотики для \texttt{get} (бинарный поиск за $O(\log n)$ на массиве против линейного поиска за $O(n)$ на списке) и разные способы пересборки при \texttt{set}. Результаты на характерных сценариях приведены в табл.~\ref{tab:array_vs_list}.

\begin{table}[H]
    \centering
    \caption{Время операций \texttt{get} и \texttt{set} двух представлений COO против quadtree (среднее по 5 запускам, профиль ShortRun)}
    \label{tab:array_vs_list}
    \footnotesize
    \begin{tabular}{lcccc}
        \toprule
        Сценарий & Операция & Quadtree & COO на массиве & COO на списке \\
        \midrule
        Синтетическая $1024\times1024$,~1\% & \texttt{get} & 28.7\,мкс & 32.2\,мкс & 545.2\,мкс \\
                                           & \texttt{set} & 349.9\,мкс & 881.9\,мс & 149.7\,мс \\
        Плотная $256\times256$              & \texttt{get} & 2.51\,мс & 4.08\,мс & 2.71\,с \\
                                           & \texttt{set} & 17.2\,мс & 8.52\,с & 310.9\,с \\
        Реальная 494\_bus ($n=2160$)         & \texttt{get} & 19.7\,мкс & 29.1\,мкс & 1.40\,мс \\
                                           & \texttt{set} & 315.3\,мкс & 140.6\,мс & 34.6\,мс \\
        Реальная zenios ($n=30064$)         & \texttt{get} & 53.8\,мкс & 51.3\,мкс & 19.9\,мс \\
                                           & \texttt{set} & 469.7\,мкс & 3.66\,с & 962.4\,мс \\
        \bottomrule
    \end{tabular}
\end{table}

Для \texttt{get} массив практически догоняет quadtree (разница 1.1--1.6 раза), тогда как список на больших $n$ отстаёт в сотни раз (на zenios~--- в 388 раз, на плотной $256\times256$~--- в 664 раза): у связного списка нет произвольного доступа, поэтому бинарный поиск невозможен. При малых $n$ (bcspwr01) разница со списком невелика, а для крошечных матриц список даже способен быть быстрее массива за счёт отсутствия аллокаций массива.

Для \texttt{set} картина обратная. COO на массиве при каждом обновлении выделяет новый массив и сдвигает блоки памяти (\texttt{Array.blit}); COO на списке пересобирает список через \texttt{cons}~--- без физического копирования элементов. На разреженных и умеренно заполненных матрицах список быстрее массива в 2.6--7 раз (494\_bus~--- в 4 раза, cryg2500~--- в 7 раз; см. табл.~\ref{tab:real_results}). На полностью плотной матрице $256\times256$ ($n=65536$) перевешивает масштаб: список пересоздаёт десятки тысяч cons-ячеек при каждой записи и проигрывает массиву в 36 раз (310.9\,с против 8.52\,с). Таким образом, выбор представления зависит от характера матрицы: для операций с преобладанием \texttt{get} предпочтителен массив, для композиции матриц из точечных обновлений~--- список.

\section{Анализ асимптотической сложности}

Для каждого формата определена ожидаемая асимптотическая сложность основных операций. Пусть $N$~--- размер стороны матрицы, $n$~--- число ненулевых элементов, $k$~--- глубина дерева ($k = \log_2 N$).

\begin{table}[H]
    \centering
    \caption{Ожидаемая асимптотическая сложность операций}
    \label{tab:complexity}
    \begin{tabular}{lcc}
        \toprule
        Операция & COO & QuadTree \\
        \midrule
        \texttt{get} (доступ)        & $O(\log n)$ & $O(\log N)$ \\
        \texttt{set} (обновление)    & $O(n)$      & $O(\log N)$ \\
        \texttt{map} (отображение)   & $O(n)$      & $O(T)$      \\
        \texttt{mapi}                & $O(N^2)$      & $O(T)$      \\
        \texttt{map2} (две матрицы)  & $O(n_1 + n_2)$ & $O(T_1 + T_2)$ \\
        \texttt{mxm} (умножение)     & $O(n_1 \cdot n_2 / N)$ & $O(N^{\log_2 8})$ \\
        \bottomrule
    \end{tabular}
\end{table}

Здесь $n$~--- число ненулевых элементов матрицы, $T$~--- число узлов дерева (с учётом конденсации), $n_1$, $n_2$~--- числа ненулевых элементов умножаемых матриц.

Ключевые отличия.

\begin{itemize}
    \item \textbf{Доступ и обновление.} В COO поиск элемента~--- бинарный поиск по отсортированному массиву за $O(\log n)$; обновление, помимо поиска, требует вставки в середину массива со сдвигом за $O(n)$. В quadtree доступ~--- рекурсивный спуск по дереву за $O(\log N)$, что быстрее для плотных матриц, где $n \approx N^2$.
    \item \textbf{Отображение (map).} COO обходит только $n$ троек, quadtree~--- все $T$ узлов дерева. Если матрица сильно разреженная и дерево сильно сжато ($T \ll N^2$), quadtree может быть быстрее. Если дерево не сжимается (плотные данные), quadtree обходит все $N^2$ элементов.
    \item \textbf{Умножение матриц.} COO использует стратегию «умножение строк на столбцы» с группировкой по строкам: сложность $O(n_1 \cdot \bar{n_2})$, где $\bar{n_2}$~--- среднее число ненулевых элементов в строке второй матрицы. Quadtree использует рекурсивный divide-and-conquer: $O(N^{\log_2 8}) = O(N^3)$ в худшем случае, но с конденсацией поддеревьевреальная сложность может быть существенно ниже.
\end{itemize}

\section{Результаты}

Сводные результаты измерений на реальных матрицах приведены в табл.~\ref{tab:real_results}. В каждой ячейке указан формат и во сколько раз он быстрее в соответствующей операции.

\begin{table}[H]
    \centering
    \caption{Сравнение операций на реальных матрицах: формат и кратность ускорения (среднее по 5 независимым запускам)}
    \label{tab:real_results}
    \footnotesize
    \begin{tabular}{lrrrrrr}
        \toprule
        \multirow{2}{*}{Матрица} & \multirow{2}{*}{$N$} & \multirow{2}{*}{$n$} & \multicolumn{4}{c}{Во сколько раз быстрее формат} \\
        \cmidrule(lr){4-7}
        & & & map & mapi & get & set \\
        \midrule
        bcspwr01  &  39  &   170 & COO $\times$ 6.8 & COO $\times$ 4.4 & Quadtree $\times$ 1.8   & Quadtree $\times$ 42.5 \\
        arc130    & 130  &  1282 & COO $\times$ 6.5 & COO $\times$ 6.3 & Quadtree $\times$ 2.0   & Quadtree $\times$ 280.2 \\
        bcspwr05  & 443  &  2066 & COO $\times$ 23.5 & COO $\times$ 6.3 & Quadtree $\times$ 1.5  & Quadtree $\times$ 402.4 \\
        west0479  & 479  &  1910 & COO $\times$ 20.4 & COO $\times$ 5.8 & Quadtree $\times$ 1.5  & Quadtree $\times$ 323.0 \\
        494\_bus  & 494  &  2160 & COO $\times$ 20.0 & COO $\times$ 6.0 & Quadtree $\times$ 1.5  & Quadtree $\times$ 446.0 \\
        dwt\_992  & 992  & 17736 & COO $\times$ 4.8 & COO $\times$ 5.8 & Quadtree $\times$ 1.7  & Quadtree $\times$ 5087.5 \\
        jagmesh7  & 1138 &  8588 & COO $\times$ 10.9 & COO $\times$ 5.2 & Quadtree $\times$ 1.3  & Quadtree $\times$ 1589.5 \\
        bcspwr06  & 1454 &  6754 & COO $\times$ 27.5 & COO $\times$ 5.0 & COO $\times$ 1.1        & Quadtree $\times$ 1269.6 \\
        cryg2500  & 2500 & 12349 & COO $\times$ 15.6 & COO $\times$ 4.4 & COO $\times$ 1.3        & Quadtree $\times$ 3262.3 \\
        zenios    & 2873 & 30064 & COO $\times$ 14.2 & COO $\times$ 5.3 & COO $\times$ 1.1        & Quadtree $\times$ 7800.6 \\
        \bottomrule
    \end{tabular}
\end{table}

Операция \texttt{mxm} вынесена в отдельную табл.~\ref{tab:real_mxm}, поскольку её результат менее однозначен: на 9 из 10 матриц быстрее COO, и лишь на матрице arc130 quadtree продемонстрировал сопоставимое время.

\begin{table}[H]
    \centering
    \caption{Операция \texttt{mxm} на реальных матрицах: среднее время (мс) и соотношение (среднее по 5 запускам)}
    \label{tab:real_mxm}
    \footnotesize
    \begin{tabular}{lrrr}
        \toprule
        \multirow{2}{*}{Матрица} & \multicolumn{2}{c}{Среднее время, мс} & \multirow{2}{*}{COO быстрее в ... раз} \\
        \cmidrule(lr){2-3}
        & COO & QuadTree & \\
        \midrule
        bcspwr01  &   0.27 &      1.76 & 6.5 \\
        arc130    &  51.54 &     46.07 & 0.9 (Quadtree в 1.1 раза) \\
        bcspwr05  &   9.60 &    391.89 & 40.8 \\
        west0479  &  10.98 &    401.12 & 36.5 \\
        494\_bus  &   8.85 &    382.03 & 43.2 \\
        dwt\_992  & 289.10 &   2595.18 & 9.0 \\
        jagmesh7  &  79.59 &   2102.57 & 26.4 \\
        bcspwr06  &  47.24 &   3198.17 & 67.7 \\
        cryg2500  &  98.46 &   7919.86 & 80.4 \\
        zenios    & 480.47 & 33\,095.92 & 68.9 \\
        \bottomrule
    \end{tabular}
\end{table}

Основные наблюдения.

\begin{enumerate}
    \item \textbf{Операции \texttt{map} и \texttt{mapi}.} COO быстрее quadtree на всех десяти матрицах: в \texttt{map}~--- в 5--28 раз, в \texttt{mapi}~--- в 4--6 раз. Причина~--- в структуре данных: COO обрабатывает только $n$ троек ($O(n)$), тогда как quadtree обходит все узлы дерева ($O(T)$), а в случае \texttt{mapi} дерево и вовсе вынуждено спускаться по всем ветвям. При малых размерах матриц, когда $T$ несущественно меньше $N^2$, накладные расходы рекурсии в quadtree перевешивают теоретическое преимущество.
    \item \textbf{Операция \texttt{get}.} На массиве с бинарным поиском COO практически сравнялся с quadtree: на большинстве матриц quadtree быстрее в 1.1--2 раза, а на bcspwr06, cryg2500 и zenios COO быстрее в 1.1--1.3 раза. Благодаря доступу за $O(\log n)$ операция больше не зависит от числа троек так сильно, как в линейном варианте.
    \item \textbf{Операция \texttt{set}.} Quadtree быстрее во всех случаях: от 42.5 (bcspwr01) до $7.8\cdot10^3$ (zenios). Обновление в quadtree~--- это $O(\log N)$, тогда как в COO каждое обновление пересоздаёт отсортированный массив со вставкой со сдвигом~--- $O(n)$; относительный разрыв растёт пропорционально $n$.
    \item \textbf{Умножение матриц (\texttt{mxm}).} COO быстрее на 9 из 10 матриц (в 6.5--80 раз), и лишь на маленькой матрице arc130 quadtree примерно на 10\% быстрее. Разрежённый алгоритм \texttt{mxmcoo} выполняет группировку по строкам и перебор только ненулевых пар ($O(n_1 \cdot \bar{n}_2)$), что эффективно для реальных матриц с умеренной плотностью и структурой «почти без блочности». Рекурсивный алгоритм quadtree ($O(N^{\log_2 8})$) хорошо работал на плотных матрицах, но на разреженных реальных данных не получает достаточного выигрыша от конденсации.
\end{enumerate}

\section{Заключение}

В ходе выполнения работы получены следующие результаты.

\begin{itemize}
    \item Реализован модуль COO с операциями доступа, обновления, отображения и умножения матриц в формате Coordinate List.
    \item Реализованы мостовые функции для конвертации между форматами COO и quadtree.
    \item Написано 194 теста (39~--- для модуля COO), включая property-based тесты на FsCheck; все тесты проходят.
    \item Разработана инфраструктура измерения производительности на базе BenchmarkDotNet с методологией повторных измерений (5 независимых запусков для оценки воспроизводимости).
    \item Проведён сравнительный анализ производительности форматов COO и quadtree на синтетических (разреженных и плотных) и десяти реальных матрицах из коллекции SuiteSparse.
    \item Установлено, что COO эффективнее для операций \texttt{map}, \texttt{mapi} и \texttt{mxm} (на реальных матрицах COO выигрывает умножение на 9 из 10 матриц), тогда как quadtree эффективнее для \texttt{get} и \texttt{set} благодаря доступу за $O(\log N)$ против $O(\log n)$ (и $O(n)$ на вставку со сдвигом) в COO.
    \item Реализованы две версии формата COO~--- на отсортированном массиве (модуль \texttt{COO}) и на связном списке (модуль \texttt{COOList})~--- и измерены их различия: для \texttt{get} выигрывает массив за счёт бинарного поиска, а при композиции матрицы точечными обновлениями (\texttt{set}) версия на списке быстрее в 2.6--7 раз (вставка без копирования блоков памяти).
\end{itemize}

\begin{thebibliography}{99}
    \bibitem{qtreefsharp} Репозиторий QTreeFSharp. URL: \url{https://github.com/Lamagraph/QTreeFSharp} (дата обращения: 15.07.2026).
    \bibitem{suitesparse} SuiteSparse Matrix Collection. URL: \url{https://sparse.tamu.edu/} (дата обращения: 15.07.2026).
    \bibitem{benchmarkdotnet} BenchmarkDotNet. URL: \url{https://benchmarkdotnet.org/} (дата обращения: 15.07.2026).
    \bibitem{fsharpdocs} F Sharp Programming. URL: \url{https://en.wikibooks.org/wiki/F_Sharp_Programming} (дата обращения: 15.07.2026).
    \bibitem{matplotlib} Matplotlib documentation. URL: \url{https://matplotlib.org/stable/contents.html} (дата обращения: 15.07.2026).
\end{thebibliography}

\section{Репозиторий}

Pull request: \url{https://github.com/Lamagraph/QTreeFSharp/pull/20}

\end{document}
